# STATUS — Oravan build

> Durable sprint ledger. **Each PR updates ONLY its own sprint bullet — nothing else in this file.** Queue/merge-train state lives in orchestrator session reports, never here (the retired Now/Next/Last-updated header was the #1 merge-conflict hotspot; `.gitattributes` union-merges this file as belt-and-suspenders).
> Convention: every sprint entry records **done-ness, PR, and issues encountered** — problems are captured here first, then compounded into `docs/solutions/` when they're durable lessons (compound-engineering rule: never solve the same problem twice).
> Operating rules: rename is the ONLY gate · Claude orchestrates, Colby reviews/merges everything · subagents on Sonnet 5 · every $ decision surfaced before commit.
> Strategy of record: `docs/ideation/2026-07-05-build-gtm-strategy.md` (approved 2026-07-05).
> **Milestone 2026-07-06: ungated backlog complete** — every sprint buildable without the rename or the Stripe decision is merged (S3–S7, S9–S11, S13–S17, S21-pregen, S22–S25 + riders; 25 PRs, 0 rollbacks, suite 164→772). Remaining: S1/S2/S8/S12 (rename-gated), S18–S20 (Stripe-gated), S21 feed/CLI/ToS (Stripe-gated).
> Standing owner items (slow-moving; updated only via occasional docs PRs): 🔑 the NAME — **resolved, rename landed 2026-07** (no longer gates S1/S2/S12/S8) · HCB application (donation page + grants) · ES-reviewer recruiting (start by Aug 17) · pregen arming (PR #49 checklist, ~$3–4/mo) — **armed 2026-07-11 pending owner secrets** · Blob store for embed portraits (S15 checklist, ~cents/mo) — **armed 2026-07-11 pending owner secrets** · Stripe decision (unlocks S18–S20) — **made 2026-07-11 — S18 build started** · 🍎 Apple MCP gateway watch (docs/ideation/2026-07-11-apple-mcp-gateway-flag.md — verify tools from Apple's MCP client when testable; join Apple's connector surface the week it opens).

## Sprint tracker

Pre-sprint features (approved verdicts, shipped Jul 4, all verified in production):
- [x] Share panel — PR #27 · slug-only URLs, `lib/site.ts` created
- [x] Beta feedback pipe — PR #28 · e2e verified (issue #32), CLAUDE.md secret amendment
- [x] Call walkthrough component — PR #29 · H.R. 1787 demo, reduced-motion/SR complete
- [x] Per-bill OG cards — PR #30 · 3,334 PNGs static, +12.3s build, freshness from sync-state
- [x] Walkthrough integration — PR #31 · homepage + bill disclosure, press webm rider

Numbered sprints (S1–S25 per strategy §1.3; resequenced 2026-07-05 under rename-only-gate ruling):
- [x] S1 — Rename/noindex-lift, technical — superseded/completed by the Oravan migration track (mig S0–S8, PRs #54–#69: rename swept #57, identity/brand #55/#56/#70, noindex lifted + soft-public launch #68, decisions log #69) — see `docs/migration/decisions.md`
- [x] S2 — Identity finish — superseded/completed by the Oravan migration track (mig S0–S8, PRs #54–#69: rename swept #57, identity/brand #55/#56/#70, noindex lifted + soft-public launch #68, decisions log #69) — see `docs/migration/decisions.md`
- [x] S3 — Freshness stamp + urgency floor — PR #35, merged Jul 5 · *Issues:* none in-sprint; agent's own review caught a P1 hydration mismatch + a homepage/bills quiet-week contradiction pre-open
- [x] S4 — Homepage funnel (combined w/ S5) — PR #38, merged Jul 6 · *Issues:* merge-ref E2E failure post-#36/#37 → root cause was a **latent ZipForm hydration race on main** (input events not replayed post-hydration; valid ZIP rejected from stale state). Product fix (FormData live read). CI flake was a real slow-device user bug. → docs/solutions candidate
- [x] S5 — Donation surfaces — PR #38 (same) · built dark behind `DONATE_URL = null`; lights up when HCB lands
- [x] S6 — Spanish paired-script last mile — PR #33, merged Jul 5 · *Issues:* parity was already 304/304 — sprint honestly re-scoped to ES test coverage + 2 copy nits. Agent initially self-paused on stale premises (didn't fetch; didn't know rename-only ruling) → briefs now carry the ruling inline
- [x] S7 — Call-moment slice — PR #36, merged Jul 6 · pre-dial beat for all callers, voicemail never-apologize (test-enforced); night screen deferred per first-cut rule. *Issues:* agent briefly ran git commands in the main checkout (disclosed, restored; left package-file residue we cleaned) → "never cd into main checkout" rule added
- [x] S8 — Buffer + LAUNCH gate — superseded/completed by the Oravan migration track (mig S0–S8, PRs #54–#69: rename swept #57, identity/brand #55/#56/#70, noindex lifted + soft-public launch #68, decisions log #69) — see `docs/migration/decisions.md`
- [x] S9 — Data core + MCP scaffold — PR #34, merged Jul 5 · *Issues:* merge conflicts vs #35 (moved the functions #35 modified) → resolved by rebase agent, "location follows branch, content follows main"; #35 logic verified function-by-function into `lib/core/`
- [x] S10 — Five MCP tools + envelope — PR #37, merged Jul 6 · ZIP-only lookup w/ refine_hint (privacy-conservative cut); quiet-week tested on real corpus; PW_PORT rider ended the port-race class. Lockfile name-sync accepted here (ends 4-agent churn class)
- [x] S11 — Upstash two-DB rate limiting + CI privacy gate — PR #43, merged Jul 6 · plain-fetch clients (no SDK), salt 128-bit/24h w/ nightly dead-man's-switch, cache keys gain content-version hash, graceful in-memory fallback, self-testing key-namespace CI gate. *Issues:* agent initially refused on unverifiable authorization (docs were untracked — fixed by #41) + correctly declined nested CLAUDE.md edit (amendment orchestrator-authored instead). *Owner post-merge:* add the two counter secrets to GitHub Actions so the salt verifier arms (PR checklist)
- [ ] S12 — MCP registry + directory submissions — **in-repo half shipped, PR pending** (branch `feat/s12-registry`): `server.json` (namespace `org.oravan/mcp`, remote `https://oravan.org/api/mcp/mcp`), `scripts/check-server-json.mjs` self-test-first CI gate, `/mcp` public docs page (EN+ES, tool metadata relocated to `lib/core/mcp.ts`'s `TOOL_INFO`/`MCP_ENDPOINT_URL` so route.ts and the page share one copy), `llms.txt` MCP line, `docs/mcp-server-readme.md`. External submissions (Official Registry DNS/HTTP verification + publish, PulseMCP, Glama, Smithery, Awesome MCP Servers/mcp.so, Claude Connectors Directory) are an owner runbook — `docs/press/s12-submission-kit.md` — none run by this PR (they need real credentials/DNS/external accounts). Quiet-submit ruling, 2026-07-11: run the runbook this week; announce stays held for the late Aug–Sep 2026 window per §2 Phase 2.
- [x] S13 — Embed rep-lookup widget + loader — PR #42, merged Jul 6 · 3.6KB loader (origin-derived, subdomain-migration-ready), embed route outside locale middleware (no cookies ever), embed-scoped CSP, portraits deferred to S15 (no third-party hotlink). Adopted #40's vacancy signal mid-flight
- [x] S14 — Bill-card widget + theming — PR #47, merged Jul 6 · bill-card embed (citation, AI-decoded headline with the label traveling in-widget — never shown without a real `ai_headline`, official-title fallback otherwise), plain-language status, freshness stamp from `lib/freshness`, canonical slug-only link-out. Theming is CSS-custom-properties-only (`--oravan-accent`/`-radius`/`-font`), every value allowlist-validated in `lib/embed-theme.ts` before it reaches a style prop (hex regex for accent, closed enums mapped to hardcoded values for radius/font) — injection fixtures assert malformed values are discarded outright, never partially honored. Loader extended for widget-type + slug + theme params, still 4.7KB (well under the 5KB budget). *Issues:* none.
- [x] S15 — Embed privacy hardening + CI gates (F3) — PR #51, merged Jul 6 — branch `feat/embed-privacy-hardening` pushed, HOLD-PR mode (no `gh pr create` run; orchestrator opens into the merge train). F3: referrer-domain nomination is a single ingestion point in `app/embed/layout.tsx` (covers both current widgets and any future one), truncating a Referer to registrable-domain-only (`lib/embed-referrer.ts`) before anything persists — content-free/caller-free by construction, a third `check-key-namespaces.mjs` registry (domain-content/domain-caller/domain-raw-referer rules, self-test extended), 60-day TTL counters, documented in-code as NOMINATE-only (never auto-counted as a confirmed install). Portraits: rep-lookup widget now renders a mirrored same-origin portrait (`/embed/portrait/[bioguide]`, proxied from Vercel Blob, never a direct client-side Blob/third-party request) when available, else a CSS-only initials avatar — zero-third-party holds in both states; `scripts/mirror-portraits.mjs` is a new no-op-safe (no `BLOB_READ_WRITE_TOKEN` yet) nightly step in `sync-bills.yml`; ships with an empty `data/portrait-manifest.json` since no Blob store is provisioned yet (owner action pending, see PR body's enable checklist). New static gate `scripts/check-embed-fingerprinting.mjs` (self-test-driven, denylists fingerprinting-shaped APIs + analytics identifiers in first-party embed code; limits disclosed in-file — a grep gate, not a behavioral scanner). EN/ES parity untouched (no new user-facing strings this sprint). *Issues:* none blocking; main-site `RepCard.tsx`'s existing `unitedstates.github.io` hotlink is unchanged/out-of-scope (next/image already keeps that browser-side request same-origin there, and the citizen site never made the embed's zero-third-party claim) — flagged for awareness, not treated as a gap.
- [x] S16 — Configurator + docs + launch kit — PR #52, merged Jul 6 (→ KTD-8 outreach sends week of Oct 26, post-S16) — branch `feat/embed-configurator` pushed, PR held for the merge train · `/embeds` (both locales): pick widget, corpus-search bill picker, theme controls limited to lib/embed-theme's exact validated surface (imported, not re-declared — bill-card only, matching what shipped), live-preview iframe on the loader's real postMessage contract, snippet per public/embed.js's actual data-attribute contract; on-page docs state the privacy claims as the CI-tested truths. `docs/press/embeds-launch-kit.md`: outreach email + one-pager (advocacy-customer premise quoted verbatim) + the Referer-nominates/Colby-confirms install protocol (F3's truncation half stays S15's, described not claimed). Cold-walkthrough Done criterion automated (`tests/embeds-cold-walkthrough.spec.ts`): the configurator's own rendered snippet pasted into a genuine cross-origin host, both widgets function (live ZIP lookup; theme lands in computed style). *Issues:* the S13/S17 `embed` exclusions in proxy.ts + next.config.ts were bare prefixes and swallowed `/embeds` outright — bare path 404'd, and NEITHER frame-ancestors block matched (a header hole, caught by S17's own tree-discovery guard) → fixed to exact-segment `embed/|embed$`. Also a real mobile grid blowout (53px horizontal scroll from the snippet `<pre>`) → `min-w-0` on the grid columns. *Flagged follow-ups:* rep-lookup widget has no theming path (widget + loader param map, S15-owned files); no plain-`<iframe>` fallback snippet documented for script-tag-hostile CMSes; embed attribution is the plain "Powered by Oravan" footer link — spec-compliant wording, no badge treatment yet.
- [x] S17 — frame-ancestors split posture (F1, F2) — PR #45, merged Jul 6 · **Audit first:** S13 (#42) shipped ONLY the embed carve-out (`frame-ancestors *` on `/embed/*`, its own comment said the site-wide lock was deliberately deferred) — every non-embed route (bill pages, homepage, `/reps`, `/about`, all `app/api/*`) had NO frame-ancestors header at all, confirmed live against a built server (curl). F2 (ZIP-only, no address field in the iframe, link-out) was already satisfied by construction in #42 - no gap there. **Closed:** added the site-wide `frame-ancestors 'self'` + `X-Frame-Options` block to `next.config.ts`, source patterns mutually exclusive with the embed block by construction (verified: no CSP-header-count bleed onto `/embed/*`, which would otherwise silently re-narrow the carve-out via the browser's multi-CSP intersection rule). New `tests/frame-posture.spec.ts`: per-route-class header assertions (bill page/homepage/`/reps`/`/about`/API routes → `'self'`, embed route → carve-out), a tree-discovery regression guard (fails if a new `app/[locale]|api|embed` segment ships with no registered frame-ancestors check), and the F2 named test (real cross-origin iframe at the split-ZIP address flow, `/reps?zip=10001` — never renders the address field). *Issues:* first cross-origin-iframe test run hit the 30s test timeout exactly - a blocked subframe's navigation never fires its own `load` event, and the top document's default `waitUntil:'load'` stalls on it; fixed via `waitUntil:'domcontentloaded'` (32s → ~2s, stable across repeated runs). Extracted the ad-hoc cross-origin HTTP host helper from `tests/embed-loader.spec.ts` into `tests/helpers.ts` (`startCrossOriginHost`) so this suite and S13's both use one implementation.
- [x] S18 — Stripe + tenancy tokens — branch `feat/s18-stripe-tenancy` pushed, HOLD-PR mode (no `gh pr create` run) · third Upstash database (`UPSTASH_TENANCY_REST_*`, `lib/upstash.ts`'s `tenancyClient()`) + its own registry (`lib/tenancy.ts`: mint/hash/lookup/provision/revoke, hashed-token storage, fail-**closed** reads — the one deliberate divergence from the rest of the codebase's fail-open doctrine, commented bluntly in-code) — a fourth `scripts/check-key-namespaces.mjs` registry (env/client confinement + a tenancy-caller rule, self-test extended). `app/api/stripe/webhook/route.ts`: POST-only, Node runtime (default), `node:crypto`-only HMAC-SHA256 signature verification (no `stripe` SDK — zero-new-supply-chain posture per S11/S15 precedent), 300s replay window, `SET NX EX 7d` idempotency (same primitive as the S11 salt), handles `checkout.session.completed` (provision; returning-customer upgrades keep the existing token) / `customer.subscription.updated` (past_due/unpaid/canceled/unmapped → revoke) / `customer.subscription.deleted` (unconditional revoke) — `invoice.payment_failed` and `STRIPE_SECRET_KEY` both deliberately not built, justified in-code. **Dark-shipped**: `STRIPE_WEBHOOK_SECRET` unset in every environment today — the route refuses everything (503, body/headers never touched) rather than degrading, the one place absence-of-config means refuse not degrade. Zero new user-facing strings (Payment Links are Stripe-hosted; no pricing UI). *Issues:* fresh-eyes review (adversarial pass on a main-merged tree, full Playwright suite + all DoD gates re-run green) found and fixed three related reliability/correctness gaps, all in the claim/token lifecycle, none in signature verification (HMAC-SHA256 constant-time compare, v1-only, 300s replay window, raw-body-before-parse all held up as designed) or in the two→three-DB separation (key-namespace self-test count moved 13→17 seeded fixtures / 3→4 clean samples exactly matching S18's own additions, confirmed by diffing against main's copy of the gate script). (1) The idempotency claim (`SET NX EX 7d`) was written *before* processing, so a transient Upstash error on the very next write would leave the claim standing for its full 7-day TTL; Stripe's own retry — the mechanism that exists specifically to recover from a transient failure — would then see `duplicate` and get a silent 200, permanently losing that paying customer's provisioning. Fixed with `releaseStripeEventClaim()` (best-effort `DEL`, never throws), called whenever processing returns `ok:false`; a successful delivery still short-circuits a real duplicate. (2) `provisionFromCheckout`'s "returning customer" branch kept the tenant record's `tokenHash` but never re-created the `token:<hash>` reverse-index if a prior cancellation had DELeted it — so a churn-then-resubscribe cycle under the same Stripe customer id left that customer's embed permanently broken even though they were paying again and the record itself said `active`. (3) The same gap, more consequential: `updateSubscriptionStatus` only ever DELeted the index on the way to inactive and never restored it on the way back to active — but Stripe's own default dunning/payment-retry recovery (past_due → active) fires *only* `customer.subscription.updated`, with no checkout event at all, so fix (2) alone didn't cover the most common real-world reactivation path. Both restore the SAME token's index entry (idempotent `SET`, same hash) rather than minting a new one, since the customer's original embed snippet still carries the one plaintext token that hashes to it. New coverage: 8 new tests total (tenancy-level claim-release/re-claim, churn-then-resubscribe, dunning-recovery-alone; route-level induced-single-call-failure via a new additive-only `MockUpstash.failOnCallNumber`, and a successful-delivery-still-dedupes control) — `tests/upstash-mock.ts`'s extension defaults to `null`/no-op, verified zero effect on the six other suites sharing that mock. Typecheck/lint/naming/parity/allowlist/key-namespace-self-test/embed-fingerprinting-self-test green after every fix; full 886-test Playwright suite re-verified green end-to-end post-fix (one unrelated pre-existing UI-timing flake in `tests/call-walkthrough.spec.ts` self-resolved on retry under concurrent-worktree load — not an S18 regression). *Owner arming checklist (see PR body):* Stripe products/Payment Links (Pro/Nonprofit × monthly/yearly, `metadata.tier` + `domain`/`org_name` custom fields per link), webhook endpoint URL + `STRIPE_WEBHOOK_SECRET` into Vercel env, Stripe Tax toggle. *Flagged for the orchestrator:* two CLAUDE.md amendments needed (runtime-secrets line, dynamic-routes enumeration) — not made here per house rule (Claude never edits CLAUDE.md); also flagging that this branch was found only as uncommitted working-tree state in a sibling build worktree (never committed/pushed) — reconstructed read-only for this review, committed, and pushed here as the canonical `feat/s18-stripe-tenancy`.
- [ ] S19 — Action panel paid tier + shared rate-limit arch — branch `feat/s19-action-panel` pushed, HOLD-PR mode (no `gh pr create` run) · **extends `app/api/script` in place** rather than forking a tenant-scoped route (S19 design §1) — cache sharing between a tenant request and a citizen request for the same bill/stance/locale is true by construction, not something a second route could accidentally break. `X-Oravan-Key` goes LIVE (dormant since S11): absent → today's citizen path byte-for-byte unchanged; present → the per-IP limiter still runs first (unconditional), then `resolveTenantAccess` (new shared gate, `lib/tenancy.ts` — the ONE function both this route and the embed page call, so "no token/bad token/inactive/no ToS" can never drift into two implementations), then the per-tenant limiter. Bad/revoked token and an inactive subscription both collapse to `403 {error:'unauthorized'}` (deliberately indistinguishable, fail-closed doctrine); no ToS on file is a distinct `403 {error:'tos_required'}`. A present-but-invalid token is never silently downgraded to the citizen path. `lib/tenancy.ts` gains `TenantRecord.tosAcceptedAt?` (optional-if-absent — every S18-provisioned record predates it and still round-trips), filled from Stripe's `consent_collection` payload only when the checkout event actually carries it (`lib/stripe-webhook.ts`'s `extractCheckoutSession`), with accept-and-fill-forward update semantics (never regresses set→unset on a later plan-change checkout). `lib/ratelimit.ts` gains `createTenantRateLimiter` (sibling to `createRateLimiter`, sharing a new `windowedCounterCore` to avoid duplicating the SET-NX-EX-then-INCR/in-memory-fallback logic): `embed-script` (60/10min) and `embed-script-day` (800/24h) per tenant, keyed on the RAW `tenantId` — deliberately never salted/hashed, since tenantId is already institutional/internal-only data, not a citizen identifier, and salt rotation would wrongly make a stable tenant look "new" mid-window. Both the per-IP and per-tenant limiters apply (different threat models, not redundant); a uniform `429 {error:'rate_limited'}` either way. Fifth `scripts/check-key-namespaces.mjs` rule (`counters-tenant-caller-mix`, whole-template-literal scan since the violation shape is two separate `${...}` interpolations combined in one key) — self-test count 17→19. `app/embed/action-panel/page.tsx` (new): server-side render-state resolution BEFORE the client widget ever mounts — no token / bad token / inactive tenant → generic "requires a paid plan" message; active-no-ToS → distinct hello@oravan.org message; domain-not-authorized → best-effort Referer-based check (page-only; the API's own iframe-internal fetch has no meaningful Referer to check), skipped when the allowlist is empty or the Referer is absent (can't enforce); unknown slug → the existing `billNotFound` state reused (an addition beyond the design's own table, since a real bill is needed for content regardless of tier); all pass → `components/embed/ActionPanelWidget.tsx` (new): stance → ZIP (F2, ZIP-only, no persisted preference to read) → generate → an editable `<textarea>` review step with the AI-disclosure chip rendered immediately adjacent and NEVER gated behind `brandless` (S14 precedent, no tier exception) → an inline, always-visible pre-dial note and `tel:` links, print-ordered after the script (no `<dialog>` — the ordering itself, not a modal, is what creates the "review before any tel: affordance" requirement inside a cross-origin iframe). Copy is reused verbatim from the `bill`/`home` namespaces via direct JSON import (same pattern `RepLookupWidget`/`BillCardWidget` already established) — only 5 new `embed.*` keys needed (unauthorized/tosRequired/domainNotAuthorized/seeImpact strings; the ZIP-entry prompt reuses `bill.needZip` verbatim). No outcome logging, no call counter, no impact link — cross-origin iframe storage partitioning makes it inconsistent-by-construction (same rule rep-lookup/bill-card already follow), with a link-out to the canonical bill page instead. Loader (`public/embed.js`) gains the `action-panel` widget type + `token` param forwarding (the one unavoidable place the capability token appears in a URL, per the embeds spec) — comments trimmed to keep the file at 4.95KB (was 5.02KB pre-S19, budget is 5KB; margin is now tighter and worth remembering on the next widget). *Tests:* AE5 privacy invariant re-run against the embed-originating path (named test in `tests/upstash-privacy.spec.ts`, a fresh trio of mocks including the tenancy DB for the first time in that file) proving the tenancy/counters/cache wire surfaces stay clean AND that a tenant request and a citizen request share the same cache entry; `tests/tenancy.unit.spec.ts`/`tests/ratelimit.unit.spec.ts`/`tests/stripe-webhook.unit.spec.ts` extended (tosAcceptedAt schema + accept-and-fill-forward, `resolveTenantAccess`'s five branches, the tenant limiter's raw-key/cross-instance/degradation/composition behavior, consent extraction). `/api/script` itself can't be `require()`'d in a unit spec (transitively pulls an ESM-only dep, confirmed empirically) — pinned instead as genuine e2e (`tests/embed-script-route.spec.ts`, real `request.post()` against the live server, distinct synthetic `x-forwarded-for` per test) using a **new tiny e2e-only fake-Upstash-REST bootstrap** (`tests/e2e-server.mjs`, seeded via `playwright.config.ts`'s `webServer.command`, replacing `next build && next start` with a wrapper that stands up the fake tenancy backend first) — the only way to get a genuinely *live* "valid token → passes the gate, reaches real generation (502, no live Anthropic key in this sandbox, not 403)" proof, since Playwright starts `webServer` before any `globalSetup` hook runs. Four fixture tenants (`tests/fixtures/e2e-tenant.ts`) cover all three page-level refusal branches plus the domain check live; the fourth degraded state (rate-limited) is proven at the widget level via a mocked 429 response, not by actually exhausting a real counter (avoids cross-test interference on the shared per-IP bucket). `tests/embed-loader.spec.ts`/`tests/embed-action-panel.spec.ts` (new) cover the full widget flow, both locales, a11y, zero cookies, and the loader's token-forwarding/refuses-without-one cases via genuine cross-origin hosts. Absorbed the trivial S18 follow-up (both "S18 design doc" pointer comments in the webhook route repointed to the real doc, `docs/ideation/2026-07-02-embeds-spec.md` §3); the flagged read-modify-write race in `provisionFromCheckout`/`updateSubscriptionStatus` stays deferred per the design's own reasoning (zero live exposure while `STRIPE_WEBHOOK_SECRET` stays unset everywhere) — `updateSubscriptionStatus` left completely untouched by this sprint. *Issues:* (1) a stale-closure bug in the widget's own `generate()` — reading the `zip` state variable in the same tick as `submitZip`'s `setZip()` call saw the pre-update value and wrongly no-op'd; fixed by threading the fresh ZIP value through as an explicit parameter instead of reading state. (2) a genuine-cross-origin-iframe test written as a hand-authored static `<iframe>` intermittently hit the 30s test timeout on a cold server (a statically-authored iframe makes the host page's `load` event block on the nested navigation — real browser behavior); fixed by switching that test to the loader's own JS-injection pattern, matching every other cross-origin test in this suite. (3) the new `app/embed/action-panel` route tripped `tests/frame-posture.spec.ts`'s S17 tree-discovery regression guard exactly as designed (a new `app/embed/*` segment ships with no registered frame-ancestors check) — registered it. Full 1020-test Playwright suite (both projects) green after fixes; zero regressions in the other 1000+ pre-existing tests. *Deliberately out of scope, stated in-code:* the S5a attribution-honor-system field-enforcement wiring `lib/tenancy.ts`'s own header comment flagged as "S19's job" is NOT built — the approved S19 design doc never calls for it, so it stayed unbuilt rather than being added unprompted; flagged here for an explicit owner/orchestrator call on a future sprint. *Owner arming checklist (see PR body):* Stripe Checkout `consent_collection.terms_of_service` needs a real ToS URL configured in the Stripe dashboard (document itself is S21 scope) before any tenant's `tosAcceptedAt` can ever get set for real; the 60/10min and 800/24h per-tenant numbers are disclosed as tunable, not derived from real demand (S18 is still dark-shipped, zero live tenant traffic exists yet).
- [ ] S20 — Impression counts (F6) — branch `feat/s20-impression-counts` pushed off `feat/s19-action-panel` (PR #76 still open/unmerged at branch time — verified via `gh pr view 76` before branching), HOLD-PR mode (no `gh pr create` run) · an "impression" = a server-side, token-resolved widget page load, counted at THREE call sites through one shared writer, new `lib/impressions.ts` (structurally mirroring `lib/embed-referrer.ts` — same in-memory fallback map, same single-startup-log line, same `SET NX EX` → `INCR` race guard), the counters database's THIRD key family (`<env>:imp:<tenantId>:<day>`, TTL 400d — a full trailing-12-calendar-month window readable from any day of the following month). The **action panel** counts only on its fully-authorized live-render branch (after `resolveTenantAccess` → `ok:true`, the domain check, and a real bill are all confirmed — every refusal branch, including `billNotFound`, returns before the count). **rep-lookup**/**bill-card** gain an *optional* `token` query param: absent → byte-for-byte unchanged (no limiter call, no tenancy lookup, nothing written); present-but-invalid/revoked/inactive → ALSO byte-for-byte unchanged rendering, only the counting silently no-ops — neither free-tier widget gains a new paywall. New `lib/tenancy.ts` export `activeTenantForImpression` (active-subscription check only, deliberately NO ToS gate, unlike `resolveTenantAccess`) backs both the write path and the new read route — Stripe's ToS URL isn't configured yet, and gating impression counting/reading on `tosAcceptedAt` would silently zero out every pre-arming tenant's numbers. All three write call sites schedule via `after()` so a slow/failed write never delays a response; rep-lookup/bill-card additionally cap the tenancy-database lookup itself at 30/10min per IP (new `RouteName: 'embed-impression-token'`) since a `token` param is a free-to-trigger background DB read for anyone, cost-containment only, never a render gate. New `GET /api/tenant/impressions` (`X-Oravan-Key` via `readOravanKey`, authorized through `activeTenantForImpression` — bad/missing/revoked token, inactive subscription, and an unreachable tenancy database all collapse to the same `403 {error:'unauthorized'}`, same non-distinguishing fail-closed doctrine as `resolveTenantAccess`): per-IP (20/10min, `'tenant-impressions'`) then per-tenant (60/10min, `'tenant-impressions-read'`) rate limiting, gate order mirroring `/api/script`'s; `?months=1..13` (default 13, clamped not 400'd — a reporting-window selector, not a validated content shape) aggregated server-side into calendar months via ONE `MGET` round trip (new command added to `tests/upstash-mock.ts`'s mock surface); `tenantId` never echoed in the response; `Cache-Control: private, no-store`. **Deliberate write/read asymmetry, load-bearing and tested**: writes fail open and silent (matches every other Upstash write in this repo); reads fail LOUD — an unconfigured or erroring counters database returns `503 {error:'temporarily_unavailable'}`, never a confidently-wrong number from the per-instance in-memory fallback, which would badly undercount a serverless fleet's real total. `lib/upstash.ts`'s `noteUpstashError` gained an optional third `consequenceOverride` parameter (two-arg call sites unchanged) so this one read path's log line can accurately say "failing closed" instead of the write-path default "failing open to in-memory," which would otherwise misdescribe it. Sixth `scripts/check-key-namespaces.mjs` registry (`lib/impressions.ts`; two new rules, `impression-content`/`impression-caller`, mirroring the domain-nomination family's own two-check shape; `countersClient` confinement allowlist extended) — self-test count 19→21 seeded fixtures, clean samples 5→6. `docs/ideation/2026-07-02-embeds-spec.md` §2.3 item 5 corrected in this same PR, marked `[Corrected 2026-07 (S20)]` rather than silently rewritten — its original text described a `/e/{tenant}/reps` path-based tenant ID + a daily Vercel-request-count job that was never built; rewritten to describe the actual shipped query-token mechanism. *Tests:* the named F6 acceptance test in `tests/upstash-privacy.spec.ts` ("a spoofed-traffic fixture confirms only a daily bucket survives, no caller-level trace" — a burst of forged/malformed/repeated/multi-hop `x-forwarded-for` values against a token-bearing rep-lookup load and repeated authorized action-panel loads, same tenant, proving ONE shared daily bucket regardless of spoofing volume); the existing AE5 embed-originating test extended to also call `noteImpression` and widen its key-shape regex (kept its `not.toContain` token/slug/ip assertions passing against the new key too); a separate AE5-style test for the rep-lookup/bill-card optional-token path specifically. Non-blocking-increment proof at two layers: (a) unit — new `tests/impressions.unit.spec.ts` (`MockUpstash.failWithNetworkError`, asserts `noteImpression` resolves without throwing); (b) page-level — new S20 assertions in `embed-action-panel.spec.ts`/`embed-rep-lookup.spec.ts`/`embed-bill-card.spec.ts` proving a token param never changes status/content (compared on the `<main>...</main>` markup specifically, not the raw response text — Next's own RSC payload legitimately echoes the requested query string verbatim in a trailing `<script>` tag, unrelated to S20, and comparing full response text flagged that expected difference as a false positive on first run; fixed and re-verified). The genuinely-erroring-vs-merely-unconfigured counters-DB distinction is deliberately pinned at the unit level only (disclosed in-code at each new e2e test) rather than by expanding `tests/e2e-server.mjs`'s shared, deliberately-narrow fake-Upstash bootstrap ("ONLY the tenancy database gets a live (fake) backend," per that file's own header comment) to also fake an always-erroring counters backend for the WHOLE suite — a bigger blast-radius change to shared e2e infra than this sprint's own scope. New `tests/impressions.unit.spec.ts` (write path: key builder, durable increment, independent per-tenant counters, in-memory fallback, network-failure fail-open; `noteImpressionForToken`: absent/valid/bad/inactive-token behavior, the per-IP lookup cap proven to skip the tenancy database entirely once tripped; read path: unconfigured/erroring → `{ok:false}` with accurate non-"failing open" log wording, exact month-bucket sums including a future-day-must-not-count case, `months=1`, an honest all-zero window) and new `tests/tenant-impressions.unit.spec.ts` (driven directly against the route module via a real `NextRequest`, same pattern `upstash-privacy.spec.ts` already uses for `/api/district` — this route has no ESM-only dependency either: auth's four fail-closed-and-indistinguishable branches, a no-ToS-tenant still authorizing, cross-tenant isolation asserted on the exact `MGET` key arguments per request, both rate limiters, the 503 read-failure path, response-shape pinning incl. `tenantId` never echoed, `?months` clamping). `tests/frame-posture.spec.ts` gained the new `app/api/tenant` route registration (required by its own tree-discovery regression guard). Zero new `messages/*.json` keys — parity gate: non-event, checked not skipped (`disclosure`/`measurementBasis` are API/machine-facing prose consumed by a tenant's own tooling, not rendered UI copy, same precedent as every other route's English-only error shape). Typecheck/lint/naming/parity/allowlist/key-namespace-self-test/embed-fingerprinting-self-test/server-json all green. Full 1088-test Playwright suite (both projects) run THREE times end-to-end: (1) surfaced the two `<main>`-vs-full-response-text test bugs above (both projects, both new S20 e2e tests; everything else including 1069 other passes was clean on that same run) — fixed; (2) a full clean re-run pre-merge: 0 failed, 1073 passed, 1 flaky (a different pre-existing, unrelated test each run — `call-walkthrough.spec.ts` then `funnel.spec.ts`'s es quiet-week fallback — both self-resolved on retry, neither touches embeds/tenancy/impressions code); mid-session PR #76 (`feat/s19-action-panel`) merged to main as squash commit `b2ee8dc` — merged `origin/main` into this branch (every conflict, in files S19 and S20 both touched, resolved to "ours" after confirming byte-identical pre-S20 content against origin/main; STATUS.md's union-merge driver left one stray duplicate placeholder line, removed by hand), re-ran every gate; (3) the definitive DoD run **on the merged tree: 0 failed, 0 flaky, 1074 passed, 14 skipped** (the same pre-existing per-project skip set every prior run also showed). *Deviations from the design doc, disclosed:* (1) `noteImpressionForToken`'s actual signature is `(token: string | null, ip: string)`, not just `(token)` as the design's own pseudocode showed — the explicit `ip` parameter was needed to thread the caller IP from `headers()`/`callerIp()` in the page component into the per-IP lookup-cap limiter the design itself calls for in §1; there was no other way to implement that recommendation. (2) CLAUDE.md's architecture-line addition for the new `/api/tenant/impressions` dynamic route was NOT made — the house rule protecting that file is unconditional ("Never edit CLAUDE.md"), so this one-line addition is flagged for Colby's explicit approval alongside the PR rather than self-approved, exactly as the design itself anticipated. *Owner note, restated from the design:* the flagged assumption in §1 (whether `tosAcceptedAt` should eventually read as a general "you agreed to use Oravan embeds at all" gate rather than an AI-specific consent) is unresolved by this sprint; S5a's attribution-field *enforcement* wiring also stays deferred, untouched by S20, same as S18/S19 left it.
- [ ] S21 — Feed + admin CLI + ToS (F7 pregen: shipped #49 Jul 6, **armed 2026-07-12**) — branch `feat/s21-feed-cli-tos` pushed, HOLD-PR mode (no `gh pr create` run) · pregen's own build-time secret/direct-Upstash-write posture (top-10 x 3 stances x 2 locales via the Message Batches API) is unchanged by this sprint — only its arming status moved (owner secrets now set). This branch ships the sprint's three remaining deliverables. **Feed:** `lib/core/feed.ts` (`buildFeedPayload`/`buildFeedRss`, thin wrappers over `whatsMoving()` — zero new scoring/urgency logic) + `lib/core/feed-xml.ts` (the one genuinely new piece of logic, RSS 2.0 escaping — split into its own dependency-free module specifically so it stays unit-testable: `lib/core/feed.ts` transitively pulls `server-only` via `lib/freshness.ts`, which resolves only inside Next's own bundler and can't be `require()`'d in a unit spec — confirmed empirically here, the same class of gap S19's STATUS entry documented for `/api/script`) + four static, keyless, `force-static` routes (`app/feed/whats-moving.{json,xml}`, `app/es/feed/whats-moving.{json,xml}` — locale-explicit dotted-folder routes, not one route + `?locale=`, since a force-static handler has no request object to read a query string from at build). Auth is deliberately NONE — the underlying corpus is already 100% public; a token would add an abuse surface protecting nothing and would break RSS-to-newsletter tooling that can't carry bearer tokens. Discoverability: `<link rel="alternate" type="application/rss+xml">` on the homepage (both locales), a line in `llms.txt`, a "Feed" doc section on `/embeds`. **Admin CLI:** `scripts/tenant-admin.mjs` (thin tsx shim) + `lib/tenant-admin.ts` (logic; `main()` returns an exit code rather than calling `process.exit` itself, so it's directly unit-testable) — same split as `scripts/pregen-scripts.mjs`/`lib/pregen-runner.ts`. Six commands: `list`/`inspect`/`rotate`/`revoke`/`set-attribution`/`impressions`. `lib/tenancy.ts` gains `listTenantIds`/`listTenants` (bounded `SCAN ... MATCH <env>:tenant:*` — deliberately not a maintained index Set, zero touch to the hardened S18 webhook path, and introduces no new key shape — **zero key-namespace-gate impact**, self-test count unchanged at 21 seeded/6 clean, confirmed by running it), `rotateCapabilityToken` (new-index-before-old-index-DEL ordering, so a crash mid-rotation fails toward "old token still works" rather than "neither resolves"), and `setAttributionEntitlement` (closes a real gap: no code path before this ever set `attribution: 'none'` — `provisionFromCheckout` hardcodes `'required'` — so a hand-negotiated Network deal had no way to be recorded; writes ONLY the entitlement field, wires **no** enforcement into any widget — S5a honor-system re-deferred, not reopened, per the design's own explicit ruling). `revoke` is zero new code (literally `cancelSubscription`). Redaction pinned by test: every command shows a tokenHash truncated to 12 hex chars, **except** `rotate`'s own one-time plaintext-token banner ("copy now — never shown again"); `tenantId`/`subscriptionId` print in full. Every invocation prints a loud `Operating on keyspace: <prefix>` banner first (the `VERCEL_ENV`-unset-means-dev footgun, flagged explicitly in-code), and every mutating command additionally requires `--yes`. Bundle safety is structural (nothing under `app/`/`components/` imports `lib/tenant-admin.ts`) and pinned by a grep self-test mirroring `tests/pregen-route-posture.unit.spec.ts`'s pattern. **ToS:** `app/[locale]/embeds/terms/page.tsx` (`/embeds/terms`, both locales, registered in `sitemap.ts` + hreflang), a separate document from the citizen `/terms`, with an explicit governing-language clause in BOTH languages (English controls; Spanish is a courtesy translation) and a literal `[FOUNDER: fill]` placeholder for governing law/jurisdiction — not invented here. Covers scope, nonpartisan-use, attribution/AI-label rules, prohibited uses (CSS/JS injection, scraping, sublicensing), token responsibility + best-effort domain allowlist + Oravan's rotate/revoke rights (directly operationalized by the CLI above), the AllSides/lean/coverage-data exclusion, Stripe billing, no-warranty/freshness disclaimer, contact, and a changes/versioning note. Deliberately **no** `tosVersion` field added to `TenantRecord` (YAGNI; `tosAcceptedAt`'s hardened S18/S19 logic stays untouched). **FLAGGED LOUDLY, per the design's own instruction: this ToS is AI-drafted legal text and must be reviewed by the founder — ideally a lawyer — before any tenant is permitted to accept it, i.e. before Stripe Checkout's `consent_collection.terms_of_service` is pointed at this page's live English URL and before any Payment Link goes live.** *Tests:* `tests/tenant-feed.unit.spec.ts` (the dependency-free XML-escaping module: all 5 XML-significant characters, a hostile combined-injection fixture proving no double-escaping, RFC 822 date formatting incl. an unparseable-date fallback); `tests/tenant-feed.spec.ts` (genuine e2e against the built server for all 4 routes — content-type/cache headers, zero cookies, item-count parity between JSON and XML, the per-item AI-label invariant, locale parity, a spoofed-`X-Forwarded-For` probe proving zero caller-derived material reaches the response, discoverability links); `tests/tenant-admin.unit.spec.ts` (all six commands against `MockUpstash`, extended this sprint with `SCAN` support; redaction pinned on every formatter; `--yes` gating; loud env refusal; the bundle-safety grep self-test; a genuine subprocess run of `check-key-namespaces.mjs` proving the new tenancy functions add zero gate surface); `tests/embeds-terms.spec.ts` (single h1, governing-language notice, every required section heading, the nonpartisan/licensing-exclusion bodies, both outbound links, the literal `[FOUNDER: fill]` placeholder in both locales); `tests/hreflang.spec.ts`/`tests/sitemap.spec.ts` extended (`/embeds/terms` added to the hreflang crawl and the static-path count, 12→13; a new llms.txt assertion for the feed line). Full DoD green: typecheck/lint/naming/parity/allowlist/key-namespace-self-test/embed-fingerprinting-self-test/server-json, and the full Playwright suite (both projects) on this main-merged branch: **0 failed, 14 skipped, 1170 passed** (was 1074 passed/14 skipped pre-S21-remainder — +96 across both projects, i.e. +48 new tests, matching the new/extended spec files). *Deviations from the design doc:* none structural — the design's own file list was followed exactly. One wording fix found mid-build: `lib/tenant-admin.ts`'s refusal-message strings and usage text could not literally spell out `UPSTASH_TENANCY_REST_*`/`UPSTASH_COUNTERS_REST_*` — the key-namespace gate's env-confinement rule fires on that substring appearing ANYWHERE outside `lib/upstash.ts`, including a comment or string constant, not just a real `process.env` read — reworded to name the databases ("the TENANCY database") with a pointer to `lib/upstash.ts` for the exact variable names; two new CLI-only helpers, `tenancyConfigured()`/`countersConfigured()`, were added to `lib/upstash.ts` itself (the one file allowed to hold those literals) so the CLI can still refuse loudly without duplicating the check. *Owner items (see PR body):* (1) point Stripe Checkout's `consent_collection.terms_of_service` at `https://oravan.org/embeds/terms` (the English URL, regardless of buyer locale) — **only after founder/legal review of the ToS text**; (2) `scripts/tenant-admin.mjs` usage examples and the `VERCEL_ENV` keyspace footgun are documented in the PR body; (3) governing law/jurisdiction (ToS §9) is an explicit `[FOUNDER: fill]` placeholder, unresolved by design — founder/legal decision, out of any agent's authority to invent.
- [x] S22 — JSON-LD + hreflang + sitemap/robots/llms.txt — PR #39, merged Jul 6 · *Findings:* #30's hreflang covered bills only; /impact structurally lacked metadata (split to server wrapper); x-default added; URL-building centralized after validator caught homepage canonical/sitemap disagreement. noindex provably untouched
- [x] S23 — Citability/correction page + ES redistribution spot-check — PR #46, merged Jul 6 · `/citations` (both locales), footer-reachable from every page incl. bill pages, correction path reuses the existing beta-feedback intake (`#feedback` anchor into Footer's own dialog, no parallel form). `lib/core/mcp.ts`'s `SOURCE`/`AI_LABEL_TEXT`/`LICENSE_*` exported and quoted verbatim on the page so it can't drift from the real MCP envelope. `docs/es-spotcheck-redistribution.md` assembles a real 12-bill sample (4 high-urgency, 4 recently-decoded, 4 older/settled) + rubric (legal-meaning accuracy, register/neutrality, label presence) + pass/fail criteria + a log section — no live native-speaker pass run yet, same interim-material pattern as S6's `docs/es-script-spotcheck.md`. *Findings:* the MCP envelope's `ai_label`/`source`/`license` fields are English-only regardless of the `locale` param requested — flagged on the page itself and in the spot-check doc as a real gap, not fixed in this PR.
- [x] S24 — Federal boundary-source hardening — PR #44, merged Jul 6 · scoped honestly as monitoring + guards, not a boundary-data migration: RDH's "What's New" page has no RSS/JSON/API (verified live) — polls RDH's own WordPress state-sitemap.xml `<lastmod>` per tracked state instead (real, scoped, machine-parseable), diffed against `data/redistricting-watch.json` and opening a `redistricting-watch`-labeled issue on change (KTD-10 tax: author-identity/rebase-retry/SHA-verify + `data-sync` concurrency + disjoint files, all inherited from `refresh-legislators.yml`). Two-clock model recorded in `docs/solutions/two-clock-district-boundaries.md` + a dated, non-blocking `::warning` tripwire (`lib/rollover-tripwire.mjs`, fires ~Dec 1, 2026) for the mandatory 119th→120th literal/dataset bump before Jan 3, 2027. *Issues:* the seeded 10-state list's LA entry was already stale vs. the strategy doc by seed time (doc said "likely, litigation delayed"; a replacement map had actually passed 2026-05-29 and was itself under fresh litigation) — corrected with sources in the seed data. Also found and fixed a latent gap in #40: the `data-vacancy` GitHub label its issue-creation step references was never created, so that step would have failed the first time a real vacancy fired — created it alongside the new `redistricting-watch` label.
- [x] S25 — Curated-triage state-architecture spec (spec-only) — PR #48, merged Jul 6, docs-only · `docs/plans/2026-07-06-state-expansion-triage-spec.md` written out of calendar sequence (executed 2026-07-06, alongside S1) per the sprint's own written brief; specs the two-gate triage-queue design (mechanical curation vs. review-before-callable), compares LegiScan/Open States-Plural/direct TX+CA APIs decision-ready (no source chosen — Open States/Plural's post-SAI360-acquisition free-tier continuity flagged unconfirmed; LegiScan per-state pricing still unconfirmed after two research passes), and confirms the ZIP→state-district lookup mostly reuses existing Census-geocoder infrastructure rather than needing a new vendor. Build itself stays parked behind §1.2's two open triggers — unchecked here on purpose.

Off-plan riders (pulled forward, rename-independent):
- [x] Data resilience: vacancy-diff + vacant UI state (§9.1(f) items 1–2 + KTD-10 tax) — PR #40, merged Jul 6 · seat-set derivation (never `terms[-1]`), seeded with the 4 real current vacancies (CA-14, FL-20, GA-13, TX-23), two-channel loud failure (≤5 vacancies → labeled issue each; >5 → exit 1 pre-commit), vacant-seat card EN/ES with no invented election claims. *Issues:* one suite run poisoned by a mid-run server death from concurrent agent load — clean isolated re-run counted, disclosed in PR. Live cron path statically validated only (crons still paused)
- [ ] RDH map-monitoring polling (§9.1(f) item 3) — folded into PR #44 (S24) above
- [x] MCP envelope/refine_hint/tool-error ES localization — PR #50, merged Jul 6 · the gap S23 found, closed same-day; pinning test flipped to assert parity live
- [ ] Traffic watch: MCP/script usage counters + daily digest — branch `feat/traffic-watch` reconstructed + verified by adversarial review 2026-07-12 (the drafting session never committed or pushed it, despite reporting "standing by" on a background test run that was not actually running; work existed only as uncommitted diffs in a sibling worktree) and pushed to origin by that review, HOLD-PR mode (no `gh pr create` run) · new `lib/usage.ts`, the counters database's FOURTH key family (`usage:mcp:<tool>:<day>`/`usage:script:<day>`, 90d TTL, content-free/caller-free except one deliberate carve-out: `tool` is drawn from a closed 5-member compile-time union the MCP SDK itself supplies, never caller input — new `usage-content`/`usage-caller` rules in `scripts/check-key-namespaces.mjs`, fixtures 21→23 seeded / 6→8 clean). Counts every MCP tool INVOCATION (`app/api/mcp/[transport]/route.ts`'s new `withUsage` wrapper, `after()`) and every real cache-miss `/api/script` generation (`after()` post-`cache.set`). New `.github/workflows/daily-metrics.yml` (cron 13:00 UTC + `workflow_dispatch`, `issues: write`) posts one comment/day on an idempotently-self-created pinned "📊 Daily metrics" issue via `scripts/daily-metrics.mjs`, opens a labeled `traffic-spike` issue when total MCP calls or script generations exceed both a floor (50/20, undated placeholders — zero real-traffic basis, pre-launch product) and 3× their trailing-7-day median (spike-checked on 2 aggregate series only, not per-tool — low/uneven per-tool volume would false-alarm). *Deliberate deviation from the read-only design this implements:* runs via `npx tsx` (not plain `node`) so `scripts/daily-metrics.mjs` can import `lib/usage.ts`'s real key-builder/read logic directly — `lib/core/mcp.ts` transitively imports `'server-only'`, unresolvable outside Next's bundler, so `lib/usage.ts` duplicates a compile-time-checked `MCP_TOOL_NAMES` array rather than importing `TOOL_NAMES` as a value; the digest job's `npm ci` step is a resulting cost the design's own draft didn't anticipate (it assumed a stdlib-only job). *Also deviates from the task wrapper's "Deliverables" line*, which named a `VERCEL_API_TOKEN` dark-ship skip and asked for owner arming notes for it — the design being implemented explicitly and permanently rejects any `VERCEL_API_TOKEN`-gated path (Vercel's Web Analytics API requires the `@vercel/analytics` client script, which CLAUDE.md's constitution permanently bans; Vercel's compliant server-side Observability has no REST API at all, dashboard-only) — built per the design, not the wrapper's paraphrase; site page-view traffic stays disclosed as unmeasured in the digest body itself, every day. *Owner item, unresolved by this branch:* whether real site pageviews are ever wanted badly enough to reconsider the no-client-analytics rule is a CLAUDE.md-constitution-level call, surfaced not decided. Full DoD green on a main-merged tree (see PR body when opened).

## Decision log (chronological)

- 2026-07-04: Feature verdicts ruled — 3 build (shipped), 5 parked w/ triggers, notifications killed. Velocity debate: "ignore, focus on building."
- 2026-07-05: Strategy doc approved as-is. Rulings: VC dropped; embeds/MCP/ES-SEO core; donations added (HCB, off-infra); TX/CA stated. Rename = only gate; build everything else now.
- 2026-07-05: KTD-8 outreach re-timed to week of Oct 26 (post-S16) — recipients need a live product.
- 2026-07-05: "I'm concerned" stance KEPT (cost analysis: savings are cents; it's the undecided on-ramp).
- 2026-07-05: Lockfile package-name sync (pre-migration naming) accepted via #37 (ends recurring churn; rename PR loses one line item).
- 2026-07-05: Full-speed subagents (no CPU throttling) — Colby watches and flags.
- 2026-07-11: Folder consolidation — local working folder is now `~/Projects/oravan`; predecessor builds archived (public build-#1 repo; build-#2 archived at `cm2489/oravan-brand-v1`).
- 2026-07-11: Calibration fork 1 (revenue goal) answered — sustainable solo income, $3–10k/mo target; the Feb 2027 decision gate measures against this bar.
- 2026-07-11: Calibration fork 2 (advocacy-customer posture) — already resolved structurally 2026-07-02: neutral utilities, no capture mechanics for anyone (`docs/ideation/2026-07-02-monetization-strategy.md` §2.1). Recorded here for completeness, not reopened.
- 2026-07-11: Calibration fork 3 (launch timing) answered — dated public launch pledge: announcement lands in the government-funding-fight window (proposed Tue Sep 22, 2026), public target of 1,000 calls by Nov 3; pledge text drafted, owner posts.
- 2026-07-11: Stripe decision — GO. S18–S21 build starts 2026-07-11; pricing per `docs/ideation/2026-07-02-embeds-spec.md` §4 ladder ($29/mo or $290/yr Pro, $15/mo or $150/yr nonprofit, network hand-negotiated). Stripe is the institutional-embeds rail only; HCB is the citizen-donations rail; the two are never conflated.
- 2026-07-11: S12 ruling — quiet-submit now (registry + directory submissions run this week; review/listing latency burns down while unannounced), the public announcement itself still holds for the late Aug–Sep window.
- 2026-07-11: Pregen + Blob portraits — both armed, pending owner secrets (~$2.50–3.80/mo pregen + ~cents/mo Blob), surfaced and approved.

## Issues & learnings ledger (compound these)

- **ZipForm hydration race** (found via #38 CI): controlled-input state can be stale vs DOM post-hydration; React replays clicks, not input events. Fix pattern: read live values via FormData at submit. → **write docs/solutions entry** (queued for next chore PR)
- **Parallel-agent test-port race** (port 3300): fixed via PW_PORT param (#37). Pattern: shared-default ports + `reuseExistingServer:false` = cross-session poisoning.
- **Agent self-parking**: subagents ending turns "waiting" on already-resolved background notifications (2 cases). Fix: briefs mandate polling result files, never ending turn on a wait.
- **Merge-ref blindness**: green-in-isolation branches can break combined; CI tests the merge ref, local suites don't. Fix: pre-open rule — merge latest main + full suite before `gh pr create`.
- **Worktree hygiene**: agents must never cd into the main checkout (1 incident, disclosed + cleaned).
- **Stale-fetch premises**: agents must `git fetch` before reasoning about repo state (1 agent argued from a 4-day-old view).
- **Workflow-referenced GitHub labels aren't auto-created**: `gh issue create --label X` fails if `X` doesn't already exist in the repo. #40 shipped `--label data-vacancy` without ever creating that label — found while building S24's `redistricting-watch` label. Both now exist; future `gh issue create --label` additions should create the label in the same PR.
- **MCP envelope was English-only regardless of `locale`**: S23's copy-vs-code comparison (PR #46) found and honestly pinned — not fixed — that `buildEnvelope`'s `source`/`ai_label`/`license` text ignored the request's `locale`, so a Spanish-locale MCP query still carried an English AI-disclosure. Closed via PR #50 (`fix/mcp-envelope-es`): `SOURCE`/`AI_LABEL_TEXT`/`LICENSE_PUBLIC_DOMAIN`/`LICENSE_AI_CONTENT` are now `Record<Locale, string>` pairs, `lookup_representatives`' `refine_hint` and every `toolError()` message in `app/api/mcp/[transport]/route.ts` got the same EN/ES treatment, `/citations` now quotes both language variants side by side (with `lang` attributes), and the PR #46 pinning test in `tests/citations.spec.ts` was flipped from asserting the gap to asserting parity. Swept the rest of the MCP surface for the same class of gap: tool `title`/`description` and zod `.describe()` schema text stay English-only on purpose (agent-facing tool metadata, never relayed to the end user verbatim - documented inline in route.ts); the `vacancies` field is structured data (`{state, district}`) with no free text, so nothing to localize there. The compound loop working as designed - S23 named the gap, this PR closed it.

## Definition-of-done addendum (every future PR)

1. Tests that must clear are listed in the sprint entry and green on a main-merged tree before the PR opens.
2. EN/ES parity in the same PR. 3. No $ commitment without a surfaced decision. 4. STATUS.md: the PR updates ONLY its own sprint bullet (done-ness, PR number, issues) — never any shared line; drift reconciliations happen in dedicated docs PRs. 5. Durable lessons go to docs/solutions/ via chore PRs.
